# landierco

## theme management
### branches
* `serif-theme` is tracking `jekyll-serif-theme/master` (created with `git reset --hard jekyll-serif-theme/master`
* `theme` contains all the customization and is rebased on `serif-theme`
* `main` is rebased on `theme`
